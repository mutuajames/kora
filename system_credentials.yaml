# Kora System Administrator Credentials
# WARNING: Change this password immediately. It is stored as plaintext on disk
# and hashed at startup via bcrypt (cost 12), kept in memory only.
# Do NOT commit this file with a real password to version control.
system_admin:
  email: admin@kora.local
  password: admin123
